#include <stdio.h>
#include <iostream>
#include <string>
using namespace std;
int main(void)
{
	int N;
	string s;
	scanf("%d", &N);
	while (N)
	{
		scanf("%s", &s);
		if (s ) //문자열에 조건문 붙이기?
	}
}