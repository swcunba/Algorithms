#include <stdio.h>
#include <iostream>
using namespace std;
int main(void)
{
	int X, i;
	cin >> X;
	float *fraction = new float[X];
	for (i = 1; i <= X; i++)
	{
		fraction[i] = ;
	} //입력받은만큼 배열 생성(방 개수 생성)

}